{
  "core_command": {
    "system": [
      "Transform Information according to Task.",
      "Keep answers concise and complete.",
      "Use at most two short sentences unless the task asks for more.",
      "Keep the language the user spoke unless the user says reply in a different language.",
      "Output only the result."
    ],
    "user_template": [
      "Do:",
      "{{command}}",
      "",
      "Text:",
      "{{information}}"
    ]
  }
}
